name: SuiteCloudPOC CLI
on:
  workflow_dispatch:
  
jobs:
  deploy-production:
    environment: production
    name: Deploy to Production (main)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          cd ./com.servicerocket.clipoc
          npm install
          npm install -g --acceptSuiteCloudSDKLicense @oracle/suitecloud-cli
          suitecloud account:savetoken -account 0000001 -authid ghaDevRole -tokenid ${{secrets.NS_TOKEN_ID}} -tokensecret ${{secrets.NS_TOKEN_SECRET}}
